/**
 * Payment Error Page - Specific handling for payment errors
 */

document.addEventListener('DOMContentLoaded', function() {
    // Initialize page handler
    const paymentErrorHandler = new PaymentHandler('error');
}); 