/**
 * Payment Cancel Page - Specific handling for canceled payments
 */

document.addEventListener('DOMContentLoaded', function() {
    // Initialize page handler
    const paymentCancelHandler = new PaymentHandler('cancel');
}); 